name: student_erp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.1.1           # Keeping the newer version of csv
  cupertino_icons: ^1.0.6
  flutter_native_splash: ^2.4.1
  http: ^1.2.2         # Keeping the newer version of http
  file_picker: ^5.5.0  # Updated to the latest version
  path_provider: ^2.1.4
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - "assets/kiotLOGO.png"
    - "assets/sakthivelsha.jpg"  # Updated to match the resolved version

flutter_native_splash:
  color: "#42a5f5"
  image: assets/coyote.png
  android_12:
    image: assets/android12splash.png
    color: "#42a5f5"
